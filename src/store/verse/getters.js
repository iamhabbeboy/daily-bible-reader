export default {
  verses(state) {
    return state.verse;
  },

  isLoading(state) {
    return state.isLoading;
  },

  pagination(state) {
    return state.pagination;
  },

  errors(state) {
    return state.errors;
  }
};
